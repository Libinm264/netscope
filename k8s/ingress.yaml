apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nexor
  namespace: nexor
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"    # SSE streams
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-buffering: "off"         # SSE — disable buffering
    cert-manager.io/cluster-issuer: "letsencrypt-prod"         # TLS via cert-manager
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - nexor.example.com
      secretName: nexor-tls
  rules:
    - host: nexor.example.com
      http:
        paths:
          # API traffic — route to Go backend
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: nexor-hub
                port:
                  name: http
          - path: /health
            pathType: Exact
            backend:
              service:
                name: nexor-hub
                port:
                  name: http
          - path: /metrics
            pathType: Exact
            backend:
              service:
                name: nexor-hub
                port:
                  name: http
          - path: /install
            pathType: Exact
            backend:
              service:
                name: nexor-hub
                port:
                  name: http
          # Everything else — Next.js dashboard
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nexor-hub-web
                port:
                  name: http
