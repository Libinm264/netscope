apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexor-hub-web
  namespace: nexor
  labels:
    app: nexor-hub-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nexor-hub-web
  template:
    metadata:
      labels:
        app: nexor-hub-web
    spec:
      containers:
        - name: hub-web
          image: ghcr.io/nexor/hub-web:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: NEXT_PUBLIC_API_URL
              value: "http://nexor-hub"
            - name: NEXT_PUBLIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: nexor-hub-secret
                  key: API_KEY
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: nexor-hub-web
  namespace: nexor
  labels:
    app: nexor-hub-web
spec:
  type: ClusterIP
  selector:
    app: nexor-hub-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
