# Build helper — run with `cargo xtask <command>` from the agent workspace root.
# NOT a member of the parent workspace.
[package]
name = "xtask"
version = "0.5.0"
edition = "2021"
publish = false

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
anyhow = "1"
